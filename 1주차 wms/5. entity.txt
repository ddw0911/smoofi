데이터베이스에서 **엔티티(Entity)**는 저장되고 관리되어야 할 정보의 단위, 즉 데이터베이스에서 의미 있는 정보를 나타내는 개체를 말합니다. 쉽게 말해, 엔티티는 데이터베이스에서 추적하고자 하는 실세계의 대상, 사건, 개념 등을 나타냅니다.

속성(Attribute): 엔티티가 가지는 세부 정보나 특성을 나타냅니다. 예를 들어, "학생" 엔티티는 이름, 학번, 생년월일 등의 속성을 가질 수 있습니다.

엔티티 타입(Entity Type): 특정 유형의 엔티티를 정의한 것으로, 하나의 엔티티 타입에는 여러 개의 엔티티 인스턴스가 존재할 수 있습니다. 예를 들어, "학생"이라는 엔티티 타입은 여러 개의 실제 학생(엔티티 인스턴스)을 포함할 수 있습니다.

고유 식별자(Primary Key): 각 엔티티 인스턴스를 유일하게 식별하기 위한 속성 또는 속성들의 조합입니다. 예를 들어, 학생 엔티티에서 학번이 고유 식별자가 될 수 있습니다.

----------------------------------------------------
1. 회원 for 회원관리, 고객센터
회원(회원id(pk), 비밀번호, 이름, 주민번호, 주소, 연락처, 이메일, 권한, 거래처명(거래처등록여부), 특이사항)
등록거래처(거래처명(pk), 거래처담당자이름, 담당자연락처, 담당자이메일, 거래처주소, 창고현황)
관리자(id(fk),사번(fk))
회원관리자(사번(pk), 부서id(fk), 회원id(fk))
관리부서(부서id(pk), 부서명, 담당업무명, 부서대표번호(연락처))

2. 로그인관리


3. 창고 for 창고관리
창고(창고id(pk), 창고이름, 창고주소, 담당자이름, 수용량, 섹션(적재위치),

4. 품목 for 입고, 출고, 재고, 재무관리
카테고리(카테고리id(pk), 카테고리명(대분류, 중분류, 소분류))
물품(품목고유번호(pk), 품목명, 카테고리id(fk), 크기, 무게, 가격, 제조사, 취급주의사항, 특이사항)

바코드생성(품목고유번호(fk), 바코드id(fk))
바코드(바코드id(pk), 품목고유번호, 품목명, 거래처명(소유업체)(fk), 저장정보(창고명, 위치), 입고날짜, 결제정보, 제품URL?)
재고(재고id(pk), 품목고유번호(fk), 바코드id(fk), 실사자사번(fk))

입고요청(입고요청id(pk), 거래처명(입고요청자)(fk), 폼목고유번호(fk))
입고지시서(지시서id(pk), 입고요청id(fk))
입고(입고id(pk), 지시서id(fk))

주문접수(주문id, 배송지정보id
배송지정보(배송지정보id, 수령자, 배송지주소, 연락처, 요청사항)

출고요청(출고요청id(pk), 
출고지시서(지시서id(pk), 운송장id(fk)
운송장(운송장id(pk), 배차id(fk)

5. 배차
배차(
배차차량(배차id(pk), 차량번호, 차종, 적재량, 운전자, 연락처, 소속(차량업체))